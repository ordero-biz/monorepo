# Copy this file to .env.local and fill in the values.
# .env.local is gitignored and auto-loaded by Next.js during development.
#
# To use a remote backend, also copy this file to .env.remote and fill in the
# remote URL. .env.remote is gitignored and loaded by pnpm dev:remote.
#
BACKEND_API_URL=
