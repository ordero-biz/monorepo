# Copy this file to .env.local and fill in the values.
# .env.local is gitignored and auto-loaded by Next.js during development.

# Optional app-local configuration for direct Next.js commands.
# Workspace dev scripts load the shared backend env files from the repository root.
BACKEND_API_URL=http://localhost:8080
