import { clientRoutes } from '@/lib/client/routes';

export const unitsOfMeasurementRootBreadcrumb = {
  href: clientRoutes.unitsOfMeasurement,
  id: 'units-of-measurement',
  label: 'Units of measurement',
} as const;
