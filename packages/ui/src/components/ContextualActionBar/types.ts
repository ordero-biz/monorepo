import type { ReactNode } from 'react';

export type ContextualActionBarRootProps = {
  ariaLabel: string;
  children: ReactNode;
};

export type ContextualActionBarLeftProps = {
  children: ReactNode;
};

export type ContextualActionBarRightProps = {
  children: ReactNode;
};
