{
  "name": "@ordero/next-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts",
    "./authPageGuard": "./src/authPageGuard.ts",
    "./server": "./src/server.ts",
    "./session": "./src/session.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "biome check .",
    "format:write": "biome check --write ."
  },
  "dependencies": {
    "@ordero/api-types": "workspace:*"
  },
  "peerDependencies": {
    "next": "^16.0.0"
  },
  "devDependencies": {
    "next": "16.1.6",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
